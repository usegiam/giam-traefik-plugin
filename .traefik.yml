# The name of your plugin as displayed in the Plugins Catalog web UI.
displayName: Giam Traefik Plugin
iconPath: ./icon.png

# For now, `middleware` is the only type available.
type: middleware

# The import path of your plugin.
import: github.com/usegiam/giam-traefik-plugin

# A brief description of what your plugin is doing.
summary: This plugin to use Giam(authorize your metrics/logs).

# Configuration data for your plugin.
# This is mandatory,
# and Plugins Catalog will try to execute the plugin with the data you provide as part of its startup validity tests.
testData:
  APIKey: ""
  LogLevel: "INFO"
  APIUrl: "https://portal.usegaim.com/api"
  GrafanaUrl: "http://observability-grafana.default.svc.cluster.local"
